# CURRENT — finance (дашборд Бластер/Култ)

Обновлено: 2026-07-29 · прод `8e6712e`

## Что это
Финансовый дашборд на данных PlanFact + amoCRM (выручка/маржа/прибыль/KPI).
Стек: Next.js 16, дев-порт 3002, прод — pm2 на 85.239.54.247 (`/opt/finance`), домен financial-dashboard.ru.
Карта проекта — `CLAUDE.md`, интеграции и секреты — `docs/integrations.md`.

## Отгружено в сессии 26–29.07 (10 коммитов)

Источник задач — созвон с Костей 21.07, разобран в **`docs/tz-dashboard-2107.md` v1.2**
(11 пунктов с таймкодами, статусы проставлены).

**Бюджеты — три сценария закрыты полностью:**
- `c1b6c9b` Бластер переведён на утверждённый «03 Бюджет 26»: было 3 657 639 прибыли за год,
  стало **474 074** — сошлось с цифрой Кости («400 тысяч»). Причина была не в коде, а в
  устаревшем конфиге.
- `5d6d3a7` Култ переведён на «03 Бюджет 2026» (в PlanFact помечен «утверждено 13.07.2026»).
  Раньше конфиг искал «01 Бюджет 2026», которого **не существует** → план май–дек был обнулён.
  Там же — **паспорт плана** в шапке: чип с составом плана по месяцам и сторож новых версий.
- `5921612` Переименование бюджета больше не роняет план: в конфиг добавлен `budgetId`
  (стабильный ключ), поиск имя → id, в паспорте чип «переименован».
- `fd25d44` Бейдж «не найден» теперь перечисляет кандидатов на замену с их датами утверждения.

**Прозрачность источников:**
- `9fe085e` Деградация источника больше не тонет за снимком. Дыра: живой битый ответ
  отбрасывался в пользу снимка, а бейджи читали `sources` **снимка** (там всегда «ok»).
  Из-за этого Култ 12 дней молча отдавал снимок от 14.07. Теперь `use-kpi` отдаёт
  `degradedSources`, у несегодняшнего снимка показывается дата + бейдж.
- `ecbeb00` + `753b9b4` Метка «источник» у каждого из 5 графиков (пункт A3 Кости).
  Тексты **сверены с кодом** — три из пяти были неверны, см. «Грабли».

**Бизнес-уравнение (A1+A2):**
- `8e6712e` Наверху «типовое для всех» без Побед и Винрейта, внизу страницы —
  «Бизнес-уравнение Лиза» со всеми столбцами. Только у Бластера (у Култа этих столбцов нет).

## In-progress
Ничего незавершённого. Дерево чистое, прод = origin = локальный HEAD.

## Next
1. **C1/C2 из ТЗ — ждут Настю, не Костю.** Вводной от Кости ждать бессмысленно (обещания
   идут два месяца, решение Саши 26.07). Настя держит источники данных. Два вопроса:
   какой источник считать «бюджетом» против «плана» (Костя развёл эти понятия на созвоне)
   и что делать с расхождением PlanFact / файл PL / файл бюджета.
2. **Cutoff Култа — методологическое решение.** Сейчас план янв–апр берётся из
   «0 Бюджет 2026», май–дек из «03» (`cutoffMonth: "2026-05"`). Это видно в паспорте.
   Если решат, что весь год должен идти по «03» — одна строка, прибыль станет
   9 053 455 вместо 7 431 190.
3. **План воронки нельзя менять из таблицы** (выяснилось при сверке меток). Запросы,
   Конверсия, Проекты, Средний чек зашиты в `lib/plans.ts`. Если Костя захочет
   менять их сам — отдельная задача: вынести в таблицу или PlanFact.

## Риски и грабли
- **Бюджет-конфиг устаревает молча.** В PlanFact версии — это НОВЫЕ записи («02» → «03»),
  а конфиг держит имя строкой. Сторож теперь предупреждает, но проверять при любом
  «цифры не те»: `getBudgets` → сверить с `entity-config.ts`.
- **Описание бюджета в PlanFact — это журнал утверждения.** Команда сама пишет туда
  «утверждено 13.07.2026». Правило «актуальный = с самой свежей датой» уже существовало,
  дашборд его просто не читал.
- **Метки источников писались по памяти и врали.** Уравнение оказалось смешанным:
  воронка — хардкод `lib/plans.ts`, деньги — бюджет PlanFact; факт так же расщеплён.
  Любые тексты «откуда цифра» сверять с кодом, а не с созвоном.
- **Снимок-fallback дважды становился ловушкой** (amoCRM 08.07, бюджет 26.07): механизм
  спасает экран, но прячет причину. Если появится третий деградирующий источник —
  сразу проверить, доходит ли его ошибка до UI.
- Расшифровки созвонов лежат готовыми в `~/Library/Application Support/GetContextMac/sessions/<UUID>/transcript.md`
  с разделением дорожек — whisper вручную гонять не нужно.
