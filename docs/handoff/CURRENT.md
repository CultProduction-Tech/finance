# CURRENT — finance (дашборд Бластер/Култ)

Обновлено: 2026-08-10 · прод `5b2be1d`

## Что это
Финансовый дашборд на данных PlanFact + amoCRM (выручка/маржа/прибыль/KPI).
Стек: Next.js 16, дев-порт 3002, прод — pm2 на **201.34.133.194** (`/opt/finance`), домен financial-dashboard.ru.
Карта проекта — `CLAUDE.md`, интеграции и секреты — `docs/integrations.md`.

⚠️ **Адрес прода сменился** между 29.07 и 10.08 (был `85.239.54.247`, сейчас мёртв и по 22, и по 443).
Хост `msk-1-vm-sgvd`, тот же ключ, тот же `/opt/finance`. Не пускает — `dig +short financial-dashboard.ru A`.

## Отгружено 10.08 (4 коммита)

Повод — вопрос «есть что-то, что мы не сделали из созвона с Костей?». Запись 21.07 перечитана
вторым проходом и сверена с кодом; ТЗ обновлено до **v1.3** (`docs/tz-dashboard-2107.md`).

- `62eb6af` **Култ переехал на бюджет от 04.08.** Команда завела новый «03 Бюджет 2026»
  («утверждено 04.08.2026»), а прежний переименовала в «03.0 … -первая версия». Поиск по
  имени увёл дашборд на новый сам — механизм 29.07 отработал в бою, — но `id` в конфиге
  остался от июльской версии и при следующем переименовании вернул бы старые цифры.
  План года: 7 431 190 → **7 662 997**.
- `eb09d91` **Метка источника стала кликабельной.** Решение №1 созвона было про ссылку
  («если таблица — ссылку на таблицу, если PlanFact — PlanFact», 21:26), а отгружен 26.07
  был текст: во всём дашборде существовали две ссылки, обе в шапке. Теперь у каждого
  графика PlanFact / воронка amoCRM своего юрлица / Google-таблица. Заодно шапки карточек
  переносятся — бейджи «без брифа» и «без даты акта» больше не наезжают на переключатель.
- `114d31d` **Плашка «месяц ещё идёт» не троится.** Претензия Кости была про дубль
  («ведь оно есть вверху», 19:20), а A2 довёл число копий до трёх. Осталось две.
- `5b2be1d` ТЗ v1.3.

## Отгружено раньше (сессия 26–29.07, 10 коммитов)

- Бюджеты, три сценария: `c1b6c9b` Бластер на «03 Бюджет 26» (3 657 639 → **474 074**,
  сошлось с «400 тысяч» Кости); `5d6d3a7` Култ на «03 Бюджет 2026» + **паспорт плана**
  в шапке со сторожем новых версий; `5921612` переименование не роняет план (`budgetId`
  как стабильный ключ); `fd25d44` бейдж «не найден» перечисляет кандидатов.
- `9fe085e` Деградация источника больше не тонет за снимком: живой битый ответ
  отбрасывался, а бейджи читали `sources` **снимка** (всегда «ok») — Култ 12 дней молча
  отдавал снимок от 14.07. Теперь `use-kpi` отдаёт `degradedSources`.
- `ecbeb00` + `753b9b4` Метка «источник» у каждого из 5 графиков; тексты сверены с кодом —
  три из пяти были неверны.
- `8e6712e` (A1+A2) Наверху «типовое для всех» без Побед и Винрейта, внизу —
  «Бизнес-уравнение Лиза» со всеми столбцами, только у Бластера.

## In-progress
Ничего незавершённого. Дерево чистое, прод = origin = локальный HEAD.

## Next
1. **C1/C2 из ТЗ — ждут Настю.** Решение №3 созвона («Костя смотрит логику источников
   и даёт вводное») не выполнено 20 дней; обещанного отдельного созвона не было.
   Два вопроса: что считать «бюджетом» против «плана» и что делать с расхождением
   PlanFact / файл PL / файл бюджета.
2. **Cutoff Култа — методологическое решение.** План янв–апр берётся из «0 Бюджет 2026»,
   май–дек из «03» (`cutoffMonth: "2026-05"`), это видно в паспорте. Если решат, что весь
   год по «03» — одна строка в `entity-config.ts`. Цифру сравнения пересчитать заново:
   прежняя (9 053 455) считалась по июльской версии бюджета, её больше нет.
3. **План воронки нельзя менять из таблицы.** Запросы, Конверсия, Проекты, Средний чек
   зашиты в `lib/plans.ts`; в подсказке к уравнению это написано прямо. Захочет Костя
   менять сам — отдельная задача (вынести в таблицу или PlanFact).

## Риски и грабли
- **Бюджет-конфиг устаревает молча — уже дважды.** 26.07 отстал на версию, 04.08 команда
  пересоздала бюджет под тем же именем. Поиск по имени спасает, `id` — нет: он задуман
  для переименования, а не для переиспользования имени. При «цифры не те» первым делом
  `getBudgets` → сверить с `entity-config.ts`.
- **Описание бюджета в PlanFact — журнал утверждения.** Команда сама пишет
  «утверждено 04.08.2026». Правило «актуальный = с самой свежей датой» существует давно.
- **Записи про сделанное врут чаще, чем записи про факты.** A3 стоял 🟢 и выглядел
  закрытым, хотя закрыт был наполовину: просили ссылку, отгрузили текст. Метки источников
  до 29.07 врали в трёх случаях из пяти. Перед «это сделано» — открыть код.
- **Снимок-fallback дважды становился ловушкой** (amoCRM 08.07, бюджет 26.07): спасает
  экран, но прячет причину. Появится третий деградирующий источник — сразу проверить,
  доходит ли его ошибка до UI.
- **`next start` при `output: standalone`** — в логах pm2 висит предупреждение Next
  («use node .next/standalone/server.js»). Не ломает, живёт так давно, но при странностях
  со сборкой смотреть сюда.
- Расшифровки созвонов лежат готовыми в `~/Library/Application Support/GetContextMac/sessions/<slug>/transcript.md`
  с разделением дорожек. Папки переименованы в `ГГГГ-ММ-ДД-ЧЧММ-slug`; созвон с Костей 21.07 —
  `2026-07-21-1406-teh-otdel-status` (в `meta.json` — `people: ["kostya"]`).
